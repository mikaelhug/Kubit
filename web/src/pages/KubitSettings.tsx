import { EmptyState } from '../components/ui'

export function KubitSettings() {
  return (
    <div class="p-6">
      <EmptyState title="Kubit settings — not built yet (M6)">
        Image Factory URL, default discovery subnets, poll interval, chart pins, and backup/restore of ~/.kubit.
      </EmptyState>
    </div>
  )
}
